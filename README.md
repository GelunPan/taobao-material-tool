# 淘宝评价素材整理工具

> 版本：**v0.5（内测版）** · 本地离线桌面工具

面向淘宝运营/评价维护场景的素材管理工具：按店铺组织评价素材记录，支持截图直接粘贴进表格、按标题搜索、一键导出 Excel；内置淘宝登录与商品素材抓取（基于 Playwright 驱动本机真实 Chrome，规避风控）。全部数据保存在本地，不上传任何内容。

---

## 功能特性

### 素材管理（核心）

- **店铺 / 产品分类两级管理（v0.5）**：店铺下可建多个**产品分类**（分类一、分类二…，树上带记录数），素材按分类归档；右键店铺 → 新增产品分类 / 重命名店铺 / 删除店铺，右键分类 → 重命名 / 删除（最后一个分类不可删，同名分类自动合并），右键空白处 → 新增店铺；点击店铺自动落到第一个分类，点分类只看该分类记录
- **商品 ▾ 菜单（v0.5）**：底部「商品 ▾」一个入口汇聚 新增商品 / 修改商品信息 / 复制商品信息
- **快捷键（v0.5）**：**Ctrl+Z 撤销上一步数据操作**（快照式，覆盖增删改/复制/图片/店铺/分类等几乎全部操作）；**Ctrl+A 全选表格**
- **批量操作（v0.5）**：框选/勾选一片区域即选中这几条**完整记录**，右键批量删除、批量复制（在原位置下方插入副本）
- **店铺管理**：新增、重命名、删除店铺；店铺树实时显示素材数量；**拖拽店铺标签可排序**（带淡入高亮动效）；双击展开/收起；**右键店铺可设置填充色和字体颜色**（按店铺持久化记忆，重启不丢），导出表单图片的标题带同色
- **素材记录管理**：每条记录含 9 个字段——商品ID、规格图、规格、标题、链接主图、补手、买家秀评价、评价图片、商品链接；支持新增、修改、**复制（在原行下方生成相同记录并弹出表单可微调）**、删除；**评价图片支持多张**，表格内并排显示
- **自适应表格**：行高随评价文字长短与图片张数自动变化（长文本自动换行、多图自动换行增高），拖动列宽或缩放窗口时自动重排；**列宽可拖拽调整、列顺序可拖动互换**
- **平滑滚动**：无极像素级滚动（非按行跳动），滚轮速度可调，滚动体验流畅
- **表格选择**：点内容单元格选中该格；点顶部字段名选中整列；点最左侧行号选中整行；**按住拖动可切换选中行**；无论光标在哪，点「修改记录」都编辑该条记录
- **批量选择**：点表格左上角的「选择」按钮进入批量选择——每行左侧出现勾选框（默认未选），底部实时显示「已选 N 条」；**勾选列表头同时出现一个「全选」复选框**，勾上即全选、取消即全不选，只选了一部分时呈半选态（半选态点击 = 全选）；再点左上角「取消选择」退出，勾选列随之隐藏
- **导出（一个入口三种出口）**：底部「导出 ▾」按钮 →
  - **导出 Excel 表格**：当前店铺当前分类全部记录导出为 `.xlsx`——首行店铺名大标题 + 表头居中 + **图片字段嵌入原图**（保持宽高比、单张显示高度/整列总宽自动封顶）+ 商品ID居中 + 链接列窄列宽存完整 URL（复制即用）；**目标文件正被 Excel/WPS 打开时给出友好占用提示**，不再裸报 Errno 13
  - **导出表单图片**：把当前分类的**整张表单完整渲染成一张图片**——滚动区外的行、全部列都在内，**图片顶部带店铺名**（右侧附记录数与时间，配色跟随店铺设置）；整体缩放至 75%（面积约 56%，清晰度与体积平衡），**导出成功后自动复制到剪贴板**，可直接 Ctrl+V；弹出保存位置，文件名默认为「店铺名_序号」，同时归档到截图历史
  - **截图历史**：查看全部归档截图（左侧缩略图列表 + 右侧按宽度铺满的预览），双击看大图；**大图支持滚轮缩放、按住左键拖动平移**，可切 适应宽度 / 适应窗口 / 实际大小；列表中右键可「查看大图」「另存为…」「在文件夹中显示」；顶栏「清空历史」二次确认后真删除；历史界面**只读**（屏蔽 Ctrl+C/右键复制/编辑/拖拽，防误操作）
- **图片粘贴**：选中图片格后直接 **Ctrl+V**（也可双击占位格或右键粘贴），剪贴板截图自动保存本地并显示缩略图；**多图格 Ctrl+V 追加不替换**；点格内「+」从文件追加多张
- **图片管理**：多张评价图片格内并排显示，最多展示 5 张，超出有「展开」小按钮；**双击缩略图查看大图**（超过 800px 自动缩放，多张可翻页）；**右键图片可单独删除该张**；对着图片按 **Del** 也可删除指定图片；**Ctrl+C 复制图片到剪贴板**
- **文本双击编辑**：双击任意文本格可就地编辑，回车保存
- **商品链接列**：固定宽度 120px，内容过长不撑宽表格
- **标题搜索**：按标题关键词实时过滤当前分类记录，支持一键清除

### 淘宝模块（v0.2 新增）

- **淘宝登录**：基于 **Playwright 驱动本机真实 Chrome** 扫码登录（非嵌入式浏览器，规避指纹风控）；只认登录后才会下发的硬标志 `unb`/`_nk_` 判登录（不认 `tracknick` 等访客也会有的 cookie），确认后访问「我的淘宝」真实验证；持久化 Chrome profile，登录态长期保留无需重复扫码
- **商品抓取**：粘贴淘宝商品链接，自动抓取**商品标题、价格、主图**（复用已登录 Chrome，先首页暖身再进商品页，反风控）；抓取结果展示标题/价格/商品ID/图片缩略图
- **一键导入**：底部「一键导入」按钮批量抓取当前店铺所有已填「商品链接」的商品信息并自动填入记录（商品ID/标题/规格/链接主图/SKU规格图）；先检测 cookie 登录态；单条失败自动跳过，不影响其他条目；已导入过的自动跳过；点击后弹出位于应用中央的加载动画实时显示进度
- **风控应对**：真实 Chrome 指纹 + 持久化 profile + 反自动化参数 + 首页暖身，降低被淘宝「访问被拒绝」拦截的概率
- **日志系统**：淘宝模块全流程操作日志（登录、抓取、异常），按天分割保存到 `data/logs/`，保留 30 天，不上传 git

### 界面与性能

- **现代界面**：统一浅色主题（卡片式面板、圆角控件、状态色按钮），支持自定义 `app/style.qss`
- **左侧栏（v0.5 重构）**：可拖拽拉伸 / 折叠，平滑过渡动画；折叠后窄轨拖出自动展开；**宽度上下限焊死在分割器子项上**（展开 73~330px、拖到下限不再误收起），拖拽边框与面板之间**只有一条 1px 分隔缝、任何操作都不会扯开**；分割条拖拽时先宽后显，店铺列表不会突然跳出
- **性能优化**：图片缩略图懒加载（仅渲染可见区域）+ 全局解码缓存（64MB），几百条记录的大店铺滚动、搜索、刷新不卡顿
- **增量渲染**：追加记录只重建新增行（不整表重建），无闪动、不打断滚动位置、保留已有勾选；配合滚动条缓动动画自然滚到新行

---

## 技术栈

| 组件 | 说明 |
| --- | --- |
| Python | 3.9+（开发验证环境 3.13） |
| PyQt6 | 桌面界面 |
| Playwright | 驱动本机真实 Chrome，淘宝登录与商品抓取 |
| openpyxl | Excel 导出 |
| requests | 图片下载 / 网络请求 |
| 数据存储 | 本地 JSON（`data/data.json`）+ 图片文件目录（`data/images/`） |

---

## 项目结构

```
taobao-material-tool/
├── main.py                    # 程序入口（启动应用、加载 QSS）
├── requirements.txt           # 依赖清单
├── README.md
└── app/
    ├── config.py              # 全局配置：路径、表格列定义、常量
    ├── storage.py             # 数据层：JSON 读写 + 店铺/产品分类/记录增删改查（店铺→分类→记录 三级模型，旧格式自动迁移）
    ├── style.qss              # 全局样式主题
    ├── assets/                # 界面图标素材（复选框三态、店铺箭头、加号、logo）
    ├── services/              # 服务层
    │   ├── __init__.py        # ImageService（图片保存/导入）、ScreenshotStore（截图归档/清空）、ExcelExporter（嵌原图）
    │   ├── history.py         # Ctrl+Z 撤销：数据快照栈（深拷贝 shops+配色，覆盖几乎全部操作）
    │   ├── taobao_session.py  # 登录态判定规则（唯一来源：只认 unb/_nk_ 硬标志）
    │   ├── taobao_client.py   # TaobaoClient：cookie 管理、登录状态检测
    │   ├── taobao_import.py   # 商品信息导入：图片下载 + 抓取结果→记录字段
    │   └── taobao_playwright.py  # Playwright 驱动真实 Chrome：登录 + 单个/批量抓取
    ├── ui/                    # 界面层
    │   ├── main_window.py     # 主窗口：界面组装与业务流程编排
    │   ├── record_table.py    # 素材表格：勾选列（含表头全选）、渲染、图片单元格、搜索
    │   ├── table_capture.py   # 整表截图：离屏副本按内容全尺寸渲染为图片
    │   ├── screenshot_history_dialog.py  # 截图历史：封面缩略图 + 按宽预览 + 大图（滚轮缩放/拖拽平移）
    │   ├── add_dialog.py      # 新增/修改素材记录弹窗
    │   ├── import_worker.py   # 一键导入后台线程（抓取+下载+组装，信号汇报进度）
    │   ├── taobao_login_dialog.py   # 淘宝登录对话框（Playwright 真实 Chrome）
    │   ├── taobao_fetch_dialog.py   # 商品抓取对话框（粘贴链接→标题/价格/主图）
    │   ├── flow_layout.py     # 流式布局：多图自动换行、行高自适应
    │   └── image_utils.py     # 图片解码 + 全局缓存：按最长边 / 按宽度（长截图）/ 顶部封面 三种口径
    └── utils/                 # 工具层
        └── logger.py          # 日志系统：按天分割、控制台+文件双输出
```

代码按「界面层 / 数据层 / 服务层 / 工具层」分层。后续新增记录字段只需修改 `app/config.py` 中的 `RECORD_FIELDS` 与 `TABLE_HEADERS`，表格渲染、搜索、导出自动跟随。

---

## 环境部署

### 环境要求

| 依赖 | 版本要求 | 说明 |
| --- | --- | --- |
| Python | 3.9+（推荐 3.11/3.13） | 开发验证环境 3.13 |
| Chrome 浏览器 | 任意较新版本 | **必须**，淘宝登录/抓取调用系统真实 Chrome（非自带 Chromium） |
| Git | 任意 | 代码版本管理 |
| 操作系统 | Windows 10/11 | 开发验证环境 Windows，理论上跨平台 |

### 完整部署步骤

```bash
# ============================================
# 1. 克隆仓库（二选一，或两个都配）
# ============================================
# GitHub 地址
git clone https://github.com/GelunPan/taobao-material-tool.git
# GitLab 地址（极狐GitLab）
git clone https://jihulab.com/gelunpan/tb-tools.git

# 进入项目目录（重要！必须在项目根目录运行后续命令）
cd taobao-material-tool

# ============================================
# 2. 创建虚拟环境（推荐，避免污染全局 Python）
# ============================================
python -m venv venv
# Windows 激活虚拟环境
venv\Scripts\activate
# macOS/Linux 激活
# source venv/bin/activate

# ============================================
# 3. 安装 Python 依赖
# ============================================
pip install -r requirements.txt
# 依赖清单：PyQt6、PyQt6-WebEngine、playwright、openpyxl、requests

# ============================================
# 4. 安装 Playwright 浏览器驱动（首次必须，淘宝模块需要）
# ============================================
playwright install chromium
# 这一步会下载 Chromium 内核（约 150MB），只需执行一次

# ============================================
# 5. 运行程序
# ============================================
python main.py
```

> **⚠️ 常见错误排查**
> - **`can't open file 'main.py'`**：不在项目目录，先 `cd taobao-material-tool`
> - **`ModuleNotFoundError: No module named 'playwright'`**：没装依赖，执行 `pip install -r requirements.txt`
> - **`ModuleNotFoundError: No module named 'PyQt6'`**：同上
> - **Playwright 报错 "Executable doesn't exist"**：没装浏览器驱动，执行 `playwright install chromium`
> - **淘宝登录打不开 Chrome**：确认本机已安装 Chrome 浏览器，且在系统 PATH 中可被识别
> - **首次启动慢**：正常，PyQt6 + Playwright 首次初始化需要几秒

---

## Git 工作流

### 双远程配置

本项目同时推送到 **GitLab（极狐）** 和 **GitHub** 两个远程，一次 push 同步到两个仓库。

```bash
# 查看当前远程配置
git remote -v

# 预期输出（origin 同时指向两个 push 地址）：
# origin  https://jihulab.com/gelunpan/tb-tools.git (fetch)
# origin  https://jihulab.com/gelunpan/tb-tools.git (push)
# origin  https://github.com/GelunPan/taobao-material-tool.git (push)

# 如果需要重新配置双远程：
git remote set-url origin https://jihulab.com/gelunpan/tb-tools.git
git remote set-url --add --push origin https://jihulab.com/gelunpan/tb-tools.git
git remote set-url --add --push origin https://github.com/GelunPan/taobao-material-tool.git
```

### 日常工作流

```bash
# ============================================
# 每天开工：先拉取最新代码
# ============================================
git pull origin main

# ============================================
# 开发：修改代码...
# ============================================

# ============================================
# 提交：查看改动 → 暂存 → 提交
# ============================================
git status                    # 查看哪些文件改了
git add -A                    # 暂存所有改动（或 git add <具体文件>）
git commit -m "feat: 描述本次改动"  # 提交

# 提交信息规范：
# feat: 新功能
# fix: 修复bug
# docs: 文档更新
# style: 样式/界面调整
# refactor: 重构（不改变功能）
# perf: 性能优化
# chore: 构建/工具/依赖调整

# ============================================
# 推送：一次 push 同步到 GitLab + GitHub
# ============================================
git push origin main

# 如果推送被拒绝（远程有新提交），先 pull 再 push：
git pull origin main
# 解决冲突后...
git push origin main
```

### 常用 Git 命令速查

```bash
git log --oneline -10        # 查看最近10条提交
git diff                      # 查看未暂存的改动
git diff --staged             # 查看已暂存的改动
git checkout -- <文件>        # 撤销某个文件的修改
git reset HEAD <文件>         # 取消暂存某个文件
git stash                     # 临时保存当前改动
git stash pop                 # 恢复临时保存的改动
git branch                    # 查看分支
git log --graph --oneline     # 图形化查看提交历史
```

### 合并冲突处理

```bash
# 1. pull 时提示冲突，先看哪些文件冲突
git status

# 2. 打开冲突文件，搜索 <<<<<<< 标记，手动解决冲突
#    <<<<<<< HEAD    （你的改动）
#    ...
#    =======        （分隔线）
#    ...
#    >>>>>>> origin/main  （远程的改动）

# 3. 解决后暂存并提交
git add <冲突文件>
git commit -m "merge: 解决冲突"

# 4. 推送
git push origin main
```

> **⚠️ 注意**：`data/` 目录（数据、图片、cookie、日志、Chrome profile）已在 `.gitignore` 中，不会被 git 跟踪，不用担心本地数据被上传。

---

## 使用说明

### 基础操作

1. **新建店铺**：点击左侧「+ 新店铺」，输入店铺名称；**右键店铺可设置填充色/字体颜色**，方便区分
2. **选择店铺**：点击左侧店铺名，右侧表格展示该店铺素材记录；**双击店铺名展开/收起**
3. **新增记录**：点「新增记录」，填写商品ID、规格、标题、补手、评价、商品链接等；规格图/链接主图各选一张，**评价图片可多次「+ 添加图片」选多张**
4. **修改/复制/删除记录**：点中该条记录任意单元格（或行号选整行），点对应按钮；也可**右键**弹出菜单操作
5. **图片操作**：复制截图后点选图片格按 **Ctrl+V** 粘贴；多图格 Ctrl+V 追加；**双击缩略图查看大图**；右键/Del 删除指定图片；Ctrl+C 复制图片
6. **文本编辑**：**双击任意文本格就地编辑**，回车保存
7. **搜索**：右上角搜索框输入标题关键词，点「搜索」过滤；「清除搜索」恢复全部
8. **批量选择**：点表格左上角「选择」按钮 → 每行左侧出现勾选框、勾选列表头出现「全选」复选框 → 勾选要操作的行（或点「全选」一键全选）；底部实时显示「已选 N 条」；再点左上角按钮退出
9. **导出**：点底部「导出 ▾」按钮，菜单里选：
   - **导出 Excel 表格**：选保存位置，生成当前店铺全部记录的 `.xlsx`
   - **导出表单图片**：选保存位置（默认文件名「店铺名_序号」），生成整张表单的完整图片（含滚动区外的行，顶部带店铺名），并自动归档到截图历史
   - **截图历史**：浏览全部归档截图；双击看大图，**大图里滚轮可缩放、按住左键可拖动平移**

> 店铺**重命名**：在左侧店铺列表上**右键 → 重命名**（左侧按钮栏只保留「+ 新店铺」「删除店铺」）。

### 淘宝登录与商品抓取

1. **淘宝登录**：点底部「淘宝登录」按钮 → 弹出登录对话框 → 点「打开 Chrome 登录」→ 自动打开真实 Chrome 窗口 → 用淘宝 APP 扫码 → 登录后自动访问「我的淘宝」**真实验证**并保存 cookie，按钮变为「淘宝已登录 ✓」
   - 已登录状态下点按钮可选择「重新登录」或「退出登录」
   - 必先扫码成功才报「登录成功」：若浏览器里残留的历史登录信息已失效，会自动清除并要求重新扫码，不会出现"假成功"
   - 「退出登录」会同时清除本地 cookie 与浏览器 profile 里的登录信息，状态可信、不留残留
   - 登录态持久化保存在 `data/taobao_chrome_profile/`，下次启动自动恢复
2. **商品抓取**：点底部「抓取商品」按钮 → 弹出抓取对话框 → 粘贴淘宝商品链接 → 点「抓取」→ 后台自动打开 Chrome（先首页暖身再进商品页）→ 展示**商品标题、价格、商品ID、主图缩略图**
   - 抓取过程不阻塞界面，在工作线程运行
   - 需先完成淘宝登录，否则会提示先登录
3. **一键导入**：点底部「一键导入」按钮 → 自动筛选当前店铺中已填「商品链接」但尚未导入的记录 → 启动一个 Chrome 会话按序抓取 → 抓取成功的自动填入商品ID/标题/规格/链接主图/SKU规格图（图片下载到本地）；失败的自动跳过；已导入过的也自动跳过
   - 点击后弹出位于应用中央的加载动画，实时显示「正在导入 N/M：链接」
   - 数据逐条落库，进程意外中断也不会丢已导入的部分
   - 单条刷新可用商品链接列右键「抓取并填充到此行」

> **风控提示**：淘宝有反爬机制，若抓取时出现「访问被拒绝」，建议：① 确保已登录且登录态有效；② 降低抓取频率，不要连续快速抓取多个商品；③ 稍等几分钟后重试。

---

## 数据安全

- 所有数据保存在程序目录下的 `data/` 中：
  - `data/data.json` — 店铺与记录数据
  - `data/images/` — 图片文件
  - `data/screenshots/` — 表单截图归档（「一键截图」「历史截图」共用；截图只新增不删除，历史截图里也没有删除入口）
  - `data/taobao_cookie.json` — 淘宝登录 cookie
  - `data/taobao_chrome_profile/` — Chrome 持久化 profile
  - `data/logs/` — 操作日志
- 图片字段记录的是本地文件路径，**移动/删除 `data/images/` 后缩略图会失效**
- `data/` 目录已在 `.gitignore` 中，不会上传到仓库
- **升级前请备份 `data/` 目录**

---

## 版本记录

### v0.5（内测版）— 产品分类 + 操作效率 + 导出打磨

**架构调整：数据模型升级为「店铺 → 产品分类 → 记录」三级结构**（`shops: {店: {分类: [记录]}}`，dict 插入顺序即展示顺序；旧格式 data.json 自动迁移，记录归入默认「分类一」）。表格/搜索/导出/新增统一走「当前店铺+当前分类」单一生入口。

- ✅ **产品分类**：店铺下多分类管理——树上二级展示 `分类名（数量）`；右键店铺/分类/空白处各有菜单（新增分类 / 重命名 / 删除，最后一个分类不可删、同名自动合并、改名保持原顺序）；点击店铺落到第一个分类，点击分类只显示该分类的记录
- ✅ **Ctrl+Z 撤销**：快照式撤销栈（深拷贝数据，不写反向操作），覆盖新增/删除/复制记录、改单元格、加删图片、批量导入外的手工操作、店铺与分类增删改名、店铺排序；Ctrl+A 全选表格
- ✅ **批量删除/复制**：框选一片区域即选中完整记录，右键批量操作；原底部「新增/修改/复制/删除」四个按钮收敛为「商品 ▾」菜单
- ✅ **Excel 导出重写**：首行店铺名大标题 + 表头居中 + 商品ID列居中；**图片字段嵌入原图文件**（非缩略图，保持宽高比，显示高度/整列总宽自动封顶）；链接列窄列宽但存**完整 URL**（复制即用）
- ✅ **导出表单图片**：整表离屏渲染 + 店铺名标题带（配色跟随店铺设置、居中显示）；**整体缩放 75%**（v0.4 的 50% 用户反馈偏糊）；**导出成功后自动复制到剪贴板**；离屏副本 `WA_DontShowOnScreen` 不再闪现「图片页面」；渲染时隐藏全部交互控件（➕/▼/勾选框/占位框）
- ✅ **左侧栏重构**：宽度上下限（73~330px）**焊死在分割器子项上**，拖边框与面板之间只有一条 1px 分隔缝，**任何拖拽/窗口缩放顺序都不会把缝扯开**（v0.4 挂在内页上会被绕过）；拖到下限不再误收起；收起/展开动画期间精确收放
- ✅ **修复**：点击产品分类不显示表单（树节点文字带「（数量）」后缀被当成分类名查询 → 空结果；真实分类名改存 UserRole）
- ✅ **修复**：新增分类自定义命名被默认名覆盖、右键重命名分类无效（同一后缀根因：右键菜单取节点文字当分类名，数据层查不到即静默失败；连带修复删除分类同类问题）
- ✅ **修复**：导出 Excel 时目标文件正被 Excel/WPS 打开报 `Errno 13 Permission denied` → 现给出「文件被占用，请关闭后重试」友好提示
- ✅ **修复**：Excel 行高随字段遍历顺序随机为 0、空图片列宽回退错误、店铺配色不持久化（现随 data.json 落盘，改名/删店同步迁移）
- ✅ **截图历史只读化**：屏蔽列表 Ctrl+C 复制文字、双击进编辑、拖拽导出、预览区右键菜单；新增「清空历史」（二次确认、默认 No、真删除）
- ✅ **修复**：分割缝往右拖完全拖不动（底部提示 QLabel 不换行时 `minimumSizeHint` = 整行文字宽度 ≈1356px，把右栏最小宽度撑爆 → 左栏开局被钉死在最小值；提示开启自动换行后恢复全区间可拖；初始栏宽设定延迟一拍，避免 showEvent 阶段布局未跑完时 `setSizes` 被等比压扁）
- ✅ **Excel 图片显示尺寸调整为原图 1/8**（`EXPORT_IMAGE_DISPLAY_SCALE=0.125`，仍嵌原图文件、保持宽高比；超高巨图仍受 533px 行高硬上限兜底）
- ✅ **商品链接识别优化**：直接粘贴整段【淘宝】分享口令（`【淘宝】https://e.tb.cn/...?tk=xxx CZ007 「标题」…`）自动识别出纯链接——新增/修改对话框落库、抓取对话框（识别结果回填输入框）、表格就地编辑（单元格回填）、链接栏抓取/填充、一键导入、Playwright 抓取入口全链路接入共享提取器 `services/link_utils.py`；裸商品 ID 等旧输入不受影响

### v0.2（内测版）— 淘宝模块

- ✅ 淘宝登录：Playwright 驱动本机真实 Chrome 扫码登录，只认 `unb`/`_nk_` 硬标志 + 我的淘宝页面真实验证，持久化 profile 长期保留登录态
- ✅ 商品抓取：粘贴链接抓取商品标题/价格/主图，复用已登录 Chrome + 首页暖身 + 反指纹，规避风控
- ✅ 一键导入：批量抓取当前店铺所有已填链接的商品信息并自动填入记录（商品ID/标题/规格/链接主图/SKU规格图），先检测 cookie 登录态，单条失败自动跳过；中央加载动画实时显示进度
- ✅ 修复：记录填满屏幕后「➕」号添加按钮被挤到可视区外、无法滚出的问题（内容末尾之外预留滚动余量，滑到底即可看到按钮）
- ✅ 修复：一键导入首次点击因后台线程状态未初始化而报错闪退
- ✅ 修复：QSS 中 `indentation` 非法属性导致的 `Unknown property indentation` 告警（缩进统一由代码设定）
- ✅ 修复：**登录判定误判** —— 原先把 `tracknick` 当登录依据，而该 cookie 未登录访客浏览淘宝时同样会下发且长期残留，导致「没登录也提示已获取 cookie」。现只认登录后才下发的硬标志 `unb`/`_nk_`，且必须通过「我的淘宝」真实验证才报成功；残留失效信息会自动清除并要求重新扫码；「退出登录」同步清除浏览器 profile 中的登录信息
- ✅ 「➕」号添加按钮图标换为 `app/assets/add_row.svg`（浅蓝圆底 + 蓝色加号）
- ✅ 优化：**添加记录不再整表重建** —— 原先每点一次「➕」都走整表 `render`（清空所有行再重建），导致画面瞬间闪动、垂直滚动位置被复位、有概率跳回第 1 条记录，且已有行的勾选状态全部丢失。现改为**增量追加**：只渲染新增行，旧行控件原样保留；追加后按缓动曲线平滑滚动到底部展示新行与「➕」按钮
- ✅ 新增：**勾选列表头「全选」复选框** —— 进入批量选择后出现在勾选列表头正中，一键全选 / 全不选；只选了一部分时呈半选态（半选态点击 = 全选，不会卡在半选）；勾选列被横向滚出视口时自动隐藏，不会遮住行号列
- ✅ 新增：**导出入口统一** —— 底部「导出 ▾」一个按钮三个出口：导出 Excel 表格 / 导出表单图片 / 截图历史（原先散落的「导出Excel」「一键截图」「历史截图」三个按钮合并）
- ✅ 新增：**导出表单图片** —— 把当前店铺整张表单（含滚出视口的行、全部列）完整渲染为 PNG，**图片顶部加一条标题带：左侧店铺名、右侧记录数与时间**；采用**离屏副本按内容全尺寸渲染**（`QWidget.grab()` 只能截可见区，直接扩高主界面表格会闪），主界面零闪动；保存对话框默认文件名「店铺名_序号」，店铺名里的 Windows 非法字符自动净化，序号按店铺递增不覆盖
- ✅ 新增：**截图历史** —— 缩略图列表 + 按宽度铺满的预览 + 双击看大图；**大图支持滚轮缩放（以鼠标位置为锚点，缩放不跳）与按住左键拖动平移**，可切 适应宽度 / 适应窗口 / 实际大小；右键「另存为…」「在文件夹中显示」
- ✅ 修复：**截图历史与大图「糊」** —— 两个根因：①列表缩略图原先按「最长边」缩放，而整表截图又宽又长（实测 1680×4237），最长边是高度，宽度被压到 **42px**，糊成一条细缝 → 改为「按宽度缩放后裁取顶部」的封面图；②预览与大图原先用「适应窗口」，长图被缩到 **16%**，字全糊 → 预览改为**按宽度铺满 + 竖向滚动**，大图默认**适应宽度**（同一张图：适应窗口 16% vs 适应宽度 60%，差 3.7 倍）并新增滚轮缩放
- ✅ 优化：**左侧栏精简** —— 删除「重命名」按钮，店铺重命名统一走**右键菜单 → 重命名**（左侧只保留「+ 新店铺」「删除店铺」）
- ✅ 修复：**日志跨天滚动报错刷屏** —— 日志文件被编辑器/杀软/索引器占用时 `os.rename` 抛 PermissionError，logging 会把整段 traceback 打到 stderr，且因 `rolloverAt` 没被推进会**每写一条日志就重试一次**；现退化为「继续追加写当前文件」并推进下次滚动时间，最坏只是跨天那天不切文件，日志内容与其它功能都不受影响
- ✅ 风控应对：真实 Chrome 指纹 + 持久化 profile + `--disable-blink-features=AutomationControlled` + 首页暖身
- ✅ 日志系统：淘宝模块全流程日志，按天分割，保留 30 天
- ✅ 店铺拖拽排序（带淡入高亮动效）
- ✅ 店铺右键设置填充色/字体颜色
- ✅ 左侧栏拖拽折叠/展开（平滑动画，右侧边框逻辑相反）
- ✅ 表格平滑无极滚动
- ✅ 多图格最多展示 5 张 + 展开/收起小按钮
- ✅ 图片 Del 键删除指定图片、Ctrl+C 复制图片
- ✅ 非图片列 Ctrl+C 复制整行记录到下方
- ✅ 商品链接列固定宽度

### v0.1（内测版）

- ✅ 首版功能：店铺管理、素材记录增删改、右键快捷菜单、图片粘贴与预览、标题搜索、Excel 导出
- ✅ 评价图片支持多张（弹窗多选、表格并排显示、双击大图翻页）；旧版单图数据自动迁移
- ✅ 双击店铺展开/收起，标题栏一键全部展开/折叠
- ⚠️ 内测说明：功能可用但未经过大规模数据验证；`data.json` 格式可能随版本调整

---

## 后续规划（Roadmap）

- [x] 抓取结果一键导入素材表格（自动填充商品ID/标题/主图/链接）
- [x] 抓取图片自动下载到本地并关联到记录
- [x] 图片嵌入 Excel 导出
- [ ] SKU 图 / 商品详情图抓取
- [ ] 批量抓取多个商品链接
- [ ] 多字段搜索与筛选（按商品ID、补手、规格等）
- [ ] 全店铺批量导出 / 多格式导出（CSV、PDF）
- [ ] 支持自定义记录字段

> 以上均为规划方向，尚未实现，以实际版本发布为准。

---

## License

项目仅作学习与内部使用，暂无开源协议（保留所有权利）。
